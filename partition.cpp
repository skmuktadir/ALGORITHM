#include<bits/stdc++.h>
using namespace std;
int partition_(int arr,int start,int end)
{
    int pivot=arr[end];

}
int main()
{
    int array[]={1,2,3,4,5};
    int size = sizeof(arr) / sizeof(arr[0]);
    partition(array,0,size);
    cout<<size;
}